document.getElementById('year').textContent=new Date().getFullYear();
const menu=document.querySelector('.menu'),nav=document.querySelector('nav');
menu.addEventListener('click',()=>nav.style.display=nav.style.display==='flex'?'none':'flex');
document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>{if(innerWidth<851)nav.style.display='none'}));
const bg=document.querySelector('.interactive-bg'),cursor=document.querySelector('.custom-cursor');
let tx=innerWidth/2,ty=innerHeight/2,cx=tx,cy=ty;
addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY;bg.style.setProperty('--mx',tx+'px');bg.style.setProperty('--my',ty+'px')});
(function animateCursor(){cx+=(tx-cx)*.18;cy+=(ty-cy)*.18;cursor.style.left=cx+'px';cursor.style.top=cy+'px';requestAnimationFrame(animateCursor)})();
document.querySelectorAll('a,button,.skill-card,.project,.time-card').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('hover'));el.addEventListener('mouseleave',()=>cursor.classList.remove('hover'))});
const canvas=document.getElementById('neural-network'),ctx=canvas.getContext('2d');let nodes=[],mouse={x:-9999,y:-9999};
function resize(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(d,0,0,d,0,0);const count=Math.min(95,Math.max(45,Math.floor(innerWidth*innerHeight/18000)));nodes=Array.from({length:count},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.24,vy:(Math.random()-.5)*.24,r:Math.random()*1.5+1}))}addEventListener('resize',resize);resize();addEventListener('pointermove',e=>{mouse.x=e.clientX;mouse.y=e.clientY});addEventListener('pointerleave',()=>mouse={x:-9999,y:-9999});
(function loop(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const n of nodes){const dx=n.x-mouse.x,dy=n.y-mouse.y,d=Math.hypot(dx,dy);if(d<170&&d>0){n.vx+=(dx/d)*.008;n.vy+=(dy/d)*.008}n.x+=n.vx;n.y+=n.vy;n.vx*=.995;n.vy*=.995;if(n.x<0||n.x>innerWidth)n.vx*=-1;if(n.y<0||n.y>innerHeight)n.vy*=-1}for(let i=0;i<nodes.length;i++){for(let j=i+1;j<nodes.length;j++){const a=nodes[i],b=nodes[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<145){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(64,165,255,${(1-d/145)*.22})`;ctx.lineWidth=.7;ctx.stroke()}}const n=nodes[i],md=Math.hypot(n.x-mouse.x,n.y-mouse.y);ctx.beginPath();ctx.arc(n.x,n.y,md<180?n.r+1:n.r,0,Math.PI*2);ctx.fillStyle=md<180?'rgba(90,225,255,.9)':'rgba(70,145,255,.55)';ctx.fill()}requestAnimationFrame(loop)})();